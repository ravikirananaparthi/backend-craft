<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }

        #login-form {
            background-color: #fff;
            padding: 20px;
            border-radius: 5px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            width: 300px;
        }

        #login-form h2 {
            text-align: center;
            margin-bottom: 20px;
        }

        #login-form input[type="text"],
        #login-form input[type="password"] {
            width: calc(100% - 20px);
            padding: 10px;
            margin-bottom: 15px;
            border: 1px solid #ccc;
            border-radius: 3px;
            box-sizing: border-box;
        }

        #login-form input[type="submit"] {
            width: 100%;
            background-color: #4caf50;
            color: #fff;
            padding: 10px;
            border: none;
            border-radius: 3px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        #login-form input[type="submit"]:hover {
            background-color: #45a049;
        }
        button {
            background-color: #008CBA; /* Blue background color */
            color: white; /* White text color */
            padding: 10px 20px; /* Padding for the button */
            border: none; /* No border */
            border-radius: 5px; /* Rounded corners */
            cursor: pointer; /* Cursor style on hover */
            transition: background-color 0.3s ease; /* Smooth transition */
        }

        button:hover {
            background-color: #005f7f; /* Darker blue on hover */
        }
        .incorrect.password.error {
    padding: 10px;
    background-color: #ffe4e1; /* Light red */
    border: 2px solid #ff7f7f; /* Darker red */
    color: #ff0000; /* Red */
    border-radius: 5px;
    font-size: 16px;
    margin: 10px 0;
    text-align: center;
    font-family: Arial, sans-serif;
    font-weight: bold;
    box-shadow: 0px 2px 4px rgba(0, 0, 0, 0.1); /* Shadow effect */
    transition: all 0.3s ease; /* Transition effect for all properties over 0.3 seconds */
}

/* Hover effect */
.incorrect.password.error:hover {
    background-color: #ffc0cb; /* Lighter red on hover */
    border-color: #ff4d4d; /* Brighter red on hover */
    color: #ff6666; /* Lighter red text on hover */
}

</style>
</head>
<body>
    <div id="login-form">
        <h2>Login</h2>
        <form action="/login" method="post">
            <input type="text" name="email" placeholder="Email" value ="<%= locals.email %>"required><br>
            <input type="password" name="password" placeholder="password" required><br>
            <p class="incorrect password error"> <%= locals.message %> </p>
            <button type="submit">Login</button>
        </form>
    </div>
</body>
</html>
